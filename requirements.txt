# LLM via Groq (kostenlose API, OpenAI-kompatibel)
openai>=1.30.0

# API-Server für iPhone PWA
fastapi>=0.111.0
uvicorn[standard]>=0.30.0
aiofiles>=23.0.0

# Natives Mac-Fenster
pywebview>=5.0.0

# Speech-to-Text (lokal, kostenlos)
faster-whisper>=1.0.0

# Text-to-Speech (lokal, kostenlos)
kokoro>=0.9.0
soundfile>=0.12.0
sounddevice>=0.4.6

# Wake Word Detection (lokal, kostenlos)
openwakeword>=0.6.0

# Audio
numpy>=1.24.0

# Memory / Database
chromadb>=0.5.0

# Lokale Embeddings (kostenlos, ~90 MB, ersetzt OpenAI Embeddings)
sentence-transformers>=3.0.0

# Browser Automation
playwright>=1.44.0

# Google APIs
google-auth>=2.29.0
google-auth-oauthlib>=1.2.0
google-auth-httplib2>=0.2.0
google-api-python-client>=2.130.0

# Notion
notion-client>=2.2.0

# Utilities
python-dotenv>=1.0.0
httpx>=0.27.0
pydantic>=2.7.0
