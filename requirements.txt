# LLM
openai>=1.30.0

# Speech-to-Text (lokal, kostenlos)
faster-whisper>=1.0.0

# Text-to-Speech (lokal, kostenlos)
kokoro>=0.9.0
soundfile>=0.12.0
sounddevice>=0.4.6

# Wake Word Detection (lokal, kostenlos)
openwakeword>=0.6.0

# Voice Activity Detection
webrtcvad>=2.0.10

# Audio
numpy>=1.24.0
pyaudio>=0.2.14

# Memory / Database
chromadb>=0.5.0

# Browser Automation
playwright>=1.44.0

# Google APIs
google-auth>=2.29.0
google-auth-oauthlib>=1.2.0
google-auth-httplib2>=0.2.0
google-api-python-client>=2.130.0

# Notion
notion-client>=2.2.0

# Utilities
python-dotenv>=1.0.0
httpx>=0.27.0
pydantic>=2.7.0

# Local LLM Fallback (via Ollama REST API – kein extra Package nötig)
# ollama läuft als lokaler Server: brew install ollama
